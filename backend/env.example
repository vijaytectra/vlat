# ===========================================
# VLAT Backend - Production Environment Variables
# Copy this to .env on your Hostinger VPS
# ===========================================

# Server Configuration
NODE_ENV=production
PORT=3000

# MongoDB Atlas Connection (use your existing MongoDB URI)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vlat?retryWrites=true&w=majority

# JWT Secret (generate a strong random string - at least 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Session Secret (generate a strong random string - at least 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Frontend URL (your frontend domain - for CORS)
FRONTEND_URL=https://vlat.leadcrm.in

# Email Configuration - Choose ONE provider:

# Option 1: Resend (Recommended - 3000 free emails/month)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com

# Option 2: Brevo (300 free emails/day)
# Get API key from: https://app.brevo.com/settings/keys/api
# BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option 3: SMTP (Nodemailer) - Works on VPS!
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Contact Form Email Recipient
# Email address where contact form submissions will be sent
CONTACT_EMAIL=vijay.r20799@gmail.com

