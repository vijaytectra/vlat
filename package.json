{
  "name": "vlat-exam",
  "version": "1.0.0",
  "type": "module",
  "description": "VLAT Exam Web Application",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "netlify-build": "npx postcss-cli css/main.css -o css/main.css",
    "vercel-build": "npm run netlify-build && node scripts/copy-to-dist.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.8"
  }
}
